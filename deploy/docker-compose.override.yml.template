# Template for docker-compose.override.yml
# This file is generated by port-allocator - DO NOT EDIT MANUALLY

version: '3.8'

services:
  web:
    ports:
      - "${WEB_PORT:-3000}:3000"
  
  api:
    ports:
      - "${API_PORT:-8080}:8080"
  
  postgres:
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
  
  redis:
    ports:
      - "${REDIS_PORT:-6379}:6379"
  
  minio:
    ports:
      - "${MINIO_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"

